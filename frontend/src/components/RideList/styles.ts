import styled from "styled-components";


export const RideListContainer = styled.div`
    display: flex;
    flex-direction: column;
    gap: 1rem 0;
    div{
        &.userData{
            margin-top: 2.5rem;
        }
    }
`